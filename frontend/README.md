# Xi Monitor Frontend

Vue 3 + TypeScript + Vite frontend for the monitor dashboard.

## Run

```powershell
cd frontend
npm install
npm run dev
```

Vite listens on `http://localhost:5173` and proxies `/api` to `http://localhost:8080`.

## Build

```powershell
npm run build
```
